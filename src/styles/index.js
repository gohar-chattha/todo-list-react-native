import common from './common-styles';
import header from './header-styles';
import home from './home-styles';
import listing from './listing-styles';

const styles = {
  common,
  header,
  home,
  listing
};

export default styles;
